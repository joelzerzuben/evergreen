import { UserPlantFilterPipe } from './user-plant-filter.pipe';

describe('UserPlantFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UserPlantFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
